spring:
  application:
    name: config-service
  cloud:
    config.server.git:
      uri: https://github.com/Abhinay8381/buildrix-ai-config-server
      username: Abhinay8381
      password: ${GITHUB_PAT:}
      default-label: main

server:
  port: 8888

eureka:
  instance:
    hostname: localhost
    prefer-ip-address: true



