spring:
  application:
    name: intelligence-service
  config:
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}

  ai:
    openai:
      api-key: ${OPENROUTER_API_KEY:sk-or-v1-placeholder}

